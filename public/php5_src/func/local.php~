<?php ## Ћокальные переменные.
function silly() { 
  $i = rand();    // записывает в $i случайное число
  echo "$i<br>";  // выводит его на экран
  // Ёта $i не имеет к глобальной $i никакого отношени€!
}
// ¬ыводит в цикле 10 случайных чисел.
for ($i=0; $i!=10; $i++) silly();
?>