## Корневой файл Блоков сайта.

# Название всего сайта.
##Title = Templier

# Автор страницы по умолчанию.
##Author = Дмитрий Котеров

# Макет страницы по умолчанию (единственный обязательный Блок).
##@Layout = default.htm

# Служебная директория, невидимая через браузер.
#
# ВНИМАНИЕ: целый ряд хостинг-провайдеров (например, net.ru) блокируют 
# имя "WEB-INF" на уровне конфигурации сервера, так что нам придется
# использовать директорию "WEBINF", а не "WEB-INF", как написано в 
# книге "PHP5". Не запутайтесь!
##WEBINF = WEBINF

# Директория поиска файлов.
##@Path = {{WEBINF}}/layout

# Директории поиска библиотек (Компонентов и Plugin-ов).
##@Lib = {{WEBINF}}/component
##@Lib = {{WEBINF}}/plugin

# Некоторые общие пути (для Модели и файлов Блоков).
##img  = {t_abs}i{/t_abs}