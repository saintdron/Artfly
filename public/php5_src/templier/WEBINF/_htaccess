# Запрещаем доступ к этой служебной директории из браузера.
deny from all
# Однако для обработчика шаблонизатора делаем исключение...
<FilesMatch "^TemplierHandler">
allow from all
SetHandler application/x-httpd-php5
# Раскомментируете, чтобы работать с PHP5, а не PHP4.
#SetHandler application/x-httpd-php5
</FilesMatch>